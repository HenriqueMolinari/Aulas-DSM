// classe especifica para rotas

abstract class AppRoutes { 
  static const String login = '/login'; //como é static é global, não precisa e não instancia a classe
  static const String register = '/register'; // '/' é rota da home
}
